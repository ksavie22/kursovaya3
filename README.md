# kursovaya3
# Анализ математической функции (C)

Данный проект представляет собой консольную программу на языке C, предназначенную для анализа заданной математической функции. Программа разработана в рамках курсового проектирования по дисциплине «Основы программирования и алгоритмизации» и позволяет выполнять основные операции исследования функции с контролем корректности входных данных и области определения.

## Постановка задачи

Разработать программу на языке C, которая позволяет вычислять значение функции в заданной точке, строить таблицу значений функции на заданном интервале, находить экстремум функции на отрезке, находить аргумент по заданному значению функции и вычислять численное значение производной функции. Программа должна иметь консольное меню, обеспечивать проверку области определения функции и контроль корректности вводимых пользователем данных.

## Математическая модель

Функция задана кусочно. При x < -2 значение функции вычисляется по формуле f(x) = 1 / ln(|x|). При -2 ≤ x < 1 значение функции определяется как f(x) = cos(x²) · e⁻ˣ. При x ≥ 1 значение функции вычисляется в виде суммы ряда f(x) = Σ (x^(2n+1) / (2n+1)), где n изменяется от 0 до 5.

## Структура проекта



## 📁 Структура проекта
# kyrsov/
# ├── kyrsov.vcxproj 
# ├── kyrsov.vcxproj.filters 
# ├── kursovaaopi.c
# ├── DocumentLayout.json 
# └── DocumentLayout.backup.json 

## Общий алгоритм работы программы

После запуска программы пользователю выводится приветственное сообщение и консольное меню. Пользователь выбирает необходимую операцию, после чего вводит требуемые данные. Программа проверяет корректность введённых значений и принадлежность аргумента области определения функции. Далее выполняется выбранная операция, результат выводится на экран, после чего пользователь может вернуться в меню или завершить работу программы.

## Описание ключевых функций

## Описание функций

### int is_defined(double x)

Функция предназначена для проверки принадлежности аргумента области определения функции.

**Принимаемые параметры:**
- `x` — вещественное число, аргумент функции.

**Возвращаемое значение:**
- `1`, если функция определена при данном значении аргумента;
- `0`, если функция не определена.

---

### double f(double x)

Функция вычисляет значение математической функции в заданной точке согласно кусочной формуле.

**Принимаемые параметры:**
- `x` — вещественное число, аргумент функции.

**Возвращаемое значение:**
- вещественное число — значение функции `f(x)`.

---

### double derivative(double x)

Функция вычисляет численное значение производной функции в заданной точке методом центральных разностей.

**Принимаемые параметры:**
- `x` — вещественное число, точка вычисления производной.

**Возвращаемое значение:**
- вещественное число — приближённое значение производной функции в точке `x`.

---

### void create_table()

Функция строит таблицу значений функции на заданном интервале.

**Принимаемые параметры:**
- отсутствуют (все данные вводятся пользователем).

**Возвращаемое значение:**
- отсутствует (результат выводится в консоль).

---

### double find_x_by_y(double Y, double a, double b)

Функция осуществляет поиск значения аргумента, при котором значение функции приблизительно равно заданному значению.

**Принимаемые параметры:**
- `Y` — заданное значение функции;
- `a` — начало интервала поиска;
- `b` — конец интервала поиска.

**Возвращаемое значение:**
- вещественное число — найденное значение аргумента `x`;
- `NAN`, если значение аргумента не найдено на заданном интервале.

---

### int find_extremum(double a, double b, double h, double* x_ext, double* f_ext)

Функция определяет экстремум функции на заданном отрезке методом перебора.

**Принимаемые параметры:**
- `a` — начало интервала;
- `b` — конец интервала;
- `h` — шаг перебора;
- `x_ext` — указатель на переменную для хранения аргумента экстремума;
- `f_ext` — указатель на переменную для хранения значения функции в экстремуме.

**Возвращаемое значение:**
- `1`, если экстремум найден;
- `0`, если экстремум отсутствует.


## Контрольные примеры
## 1.Проверка меню и приветствия 
 <img width="335" height="87" alt="Снимок экрана 2025-12-28 164107" src="https://github.com/user-attachments/assets/d8cf25cb-cd7b-4270-9db4-4037b700ff91" />
 
 ## 2.Таблица значений построена корректно 
<img width="470" height="335" alt="image" src="https://github.com/user-attachments/assets/81adf582-2803-4ea4-abd6-df6f13660687" />

 ## 3.Поиск экстремума осуществлен корректно
<img width="691" height="172" alt="image" src="https://github.com/user-attachments/assets/be82980e-d460-42d2-9cc7-c27a33817d44" />

 ## 4.Нахождение x по f(x) ~ Y  работает 
<img width="630" height="197" alt="image" src="https://github.com/user-attachments/assets/cbd025e4-5895-4e97-a889-6317e0f4684d" />

 ## 5.Производная в точке вычисляется верно 
<img width="575" height="128" alt="image" src="https://github.com/user-attachments/assets/41b7b46f-880a-47a8-b494-c76a7f62de2f" />

## Формат входных и выходных данных

Входные данные представляют собой вещественные числа типа double для аргументов, интервалов и значений функции, а также целые числа типа int для выбора пунктов меню и количества точек. Выходные данные выводятся в консоль в виде числовых значений и форматированных таблиц.

## Сборка и запуск

Для компиляции программы используется компилятор GCC. Сборка осуществляется командой gcc main.c -o function_analysis -lm. Запуск программы производится командой ./function_analysis.

## Автор

Гвардеев Станислав, группа БИЦ-252.

Проект предназначен для учебных целей.
